require 'spec_helper'

describe Question do
  
  describe :scopes do
    pending "should have a default scope created at desc"
  end
  
  describe :validations do
    pending "should validate text presence"
    pending "should validate answer presence only for persisted"
  end
  
  describe :answer do
    pending "should provide answered flag"
    pending "should be protected from answer via mass-assigment"
  end
  
end
